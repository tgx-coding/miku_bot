version: "3.8"

services:
  web:
    image: ghcr.io/tgx-coding/miku_bot:latest
    network_mode: host
    restart: always
    command: python3 QQ_Bot.py
    ports:
      - '8080:8080'
    environment:
      - test=
